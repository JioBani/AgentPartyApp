# BUG-008 · 탭을 닫아도 탭 그룹이 있던 자리에 그대로 있게 하기

## 문제와 배경

탭 그룹이 `1 2 3` 으로 놓인 상태에서 **1번 그룹의 탭 하나를 닫으면 배치가 `2 3 1` 로
바뀐다** — 닫은 그룹이 맨 뒤로 밀린다. 그룹 자체는 남아 있으므로 "빈 그룹이 정리된"
결과가 아니라, 순서만 뒤바뀌는 현상이다.

원인의 절반은 확인됐다. 레이아웃은 두 벌의 순서를 들고 있다.

- `panels` 배열 순서
- `grid`(중첩 분할 트리)의 잎 순서

화면은 격자를 따르고, **격자가 없으면 배열을 따른다**
(`Workbench.tsx` 의 `layout.grid || rowGrid(layout.panels)`).

그리고 이 둘은 정상적인 조작만으로도 어긋난다. 탭을 끌어내 새 그룹을 만들면 새 패널은
`panels` 끝에 붙지만 격자에는 다른 자리에 꽂히기 때문이다. 실제 앱에서 드래그로 그룹을
만들어 확인한 상태:

```
panels: G1[one,extra]  G2[two,three]
grid  : row(G2 G1)          ← 화면은 G2 가 왼쪽
```

즉 **격자가 사라지는 순간 화면 순서가 배열 순서로 튄다.** `1 2 3` → `2 3 1` 은 정확히
이 모양이며, 보고된 증상과 일치한다.

## 재조사 결과 (2026-09-10)

현재 소스의 **탭 닫기는 격자를 지킨다.** 실제 앱을 띄워 다음을 모두 시도했지만 순서는 유지됐다.

| 시도한 배치 | 닫은 탭 | 결과 |
| --- | --- | --- |
| 평면 3열 | 1번의 두 탭 중 하나 | 순서 유지 |
| 평면 3열 / 4열 | 1번의 마지막 탭 | 1번만 제거, 순서 유지 |
| `row(col(1 2) 3)` | 1번의 탭 | 격자 유지 |
| `col(row(1 2) 3)`, `row(1 col(2 3))` | 1번의 마지막 탭 | 올바르게 붕괴 |
| 배열 `2 3 1` / 격자 `row(1 2 3)` (어긋난 상태) | 1번의 탭 | **격자 유지** |

사용자가 증상을 본 실행 파일은 `/api/update`와 파일 버전 모두 **0.5.20**이었다. 이 버전의
`closeTab`은 탭을 닫을 때 패널에 다른 탭이 남았는지 확인하지 않고 grid leaf를 항상
삭제했다. split이 붕괴한 뒤 `reconcileGrid`가 살아 있는 패널을 바깥 row에 다시 붙이면서,
서로 달랐던 `panels` 배열 순서와 grid 표시 순서가 화면 이동으로 드러난 것이 원인이다.

이 결함은 `017c0bb`에서 `closeTab`도 다른 레이아웃 변경과 동일하게
`gridWithout(grid, before, after)`를 사용하도록 고쳤다. 즉 **실제로 비어진 패널의 leaf만
삭제**하고, `ci-sol`처럼 한 탭만 닫혀 `impl` 패널이 살아 있으면 grid를 그대로 보존한다.
수정은 0.5.22 이후 소스에 포함돼 있으며 0.5.20에는 없다.

새 E2E는 보고된 배치를 그대로 고정한다. 저장 배열은 `[오른쪽 위, 가운데, 오른쪽 아래, impl]`,
화면 grid는 `row(impl, 가운데, column(오른쪽 위, 오른쪽 아래))`인 상태에서 비활성
`ci-sol` 탭을 실제 UI로 닫고, `impl`의 grid 위치와 중첩 split이 그대로인지 확인한다.

## 곁다리로 발견된 것

드래그로 탭을 빼낼 때 **떨어뜨린 방향과 무관하게 새 그룹이 항상 왼쪽에 생긴다.**
화면 맨 오른쪽 끝(가로 98.5%)과 맨 왼쪽 끝(1.5%)에 각각 떨어뜨렸는데 두 경우 모두
`row(NEW OLD)` 가 나왔다. 다만 이 관측은 합성 포인터 이벤트로 얻은 것이라 실제 마우스
드래그와 같다고 단정할 수 없다 — 별건으로 다시 확인해야 한다.

## 원하는 동작과 범위

탭을 닫아도 남아 있는 탭 그룹은 **화면상의 자리를 유지**한다. 빈 그룹이 사라져 생긴
공간만 이웃이 나눠 갖는다(편집기에서 분할 창을 닫는 동작과 같다).

## 완료 조건

- [x] `1 2 3` → 1번 탭 닫기 → `2 3 1` 을 수정 전 실제 앱에서 재현했다.
- [x] 격자를 잃는 경로를 특정하고, 그 지점에서 근본 원인을 고쳤다 (`017c0bb`).
- [x] `panels` 순서와 격자 순서가 어긋난 상태에서 실제 UI로 비활성 탭을 닫아도
      화면 순서가 유지되는 E2E 를 추가한다(실제 앱을 띄워 API 로 조작).
- [ ] 드래그 드롭 방향이 새 그룹의 위치에 반영되는지 별도로 확인한다.

## 제외 범위와 미결 사항

현재 소스에는 추가 제품 코드 변경이 필요하지 않다. 드래그 드롭 방향 문제는 별도 항목으로
분리한다.

## 관련 근거

- 조사 브랜치: `fix/panel-order-on-tab-close`
- 근본 수정: `017c0bb` (`fix: closing a tab no longer flattens the grid`)
- 배열/격자 이중 순서: `src/renderer/workbench/Workbench.tsx`, `src/shared/workbenchGrid.ts`
- 앞서 고친 인접 결함: `fix/grid-collapse-on-tab-close` (탭을 닫으면 격자가 가로로 풀리던 문제)

## 현재 상태

- 상태: 원인 확인 · 현재 소스 수정 완료 · 정확한 보고 배치 E2E 추가
- 마지막 검토일: 2026-09-10
