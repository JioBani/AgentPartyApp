# AgentParty 현황과 분배안 (2026-08-02)

**정본은 Canopy. 이 문서는 읽기용 사본이다.**

> 📕 **착수 전에 [`공유 지점.md`](공유%20지점.md) 를 읽어라.** 워크트리를 나눠도 설치본·단위
> 테스트 중간 산출물·앱 프로세스는 **하나를 공유한다.** 2026-08-02 에 그 때문에 네 번
> 멈췄고, 네 번 다 **초록이 거짓**이었다. 특히: 의존성 폴더에 손대는 명령은 main 만,
> 단위 테스트는 순번을 받고 한 레인씩.

- `R-번호` = 요구사항, `F-번호` = 아직 요구사항으로 분해되지 않은 세부기능
- **순위**는 착수 난이도. 작을수록 쉽다. **같은 숫자는 같은 세부기능**이라 함께 하면 이득이다
- 크기가 큰 `F-` 항목도 **일단 할당한다.** 착수 첫 단계가 "요구사항 잡기"다

## 진행

| | 건수 |
|---|---|
| ✅ 완료 | **26** |
| 🔧 오구현 (고쳐야 함) | **2** |
| 🟥 미구현 (요구사항 있음) | **17** |
| 🟥 미구현 (요구사항 없음, 세부기능 덩어리) | **8** |
| ⛔ 분배 금지 (무엇을 만들지 미정) | **1** |
| **남은 일 합계** | **28** |

---

# 전체 목록 (남은 일 30줄)

표시 규칙
- **📋 기획** — 무엇을 만들지·어떤 정책으로 갈지가 아직 안 정해졌다. **착수 전에 정해야 한다**
- **🎨 디자인** — 화면을 새로 그려야 한다
- **🎨✓** — 디자인 참고가 이미 있다 (핸드오프·목업)

| 식별자 | 레인 | 위치 | 할 일 | 상태 | 순위 | 기획 | 디자인 |
|---|---|---|---|---|---|---|---|
| R-5 | A | 실행 구성 선택 | 프로바이더 그룹을 접고 편다 (처음엔 접힘) | 미구현 | 1 | | 🎨✓🔒 |
| R-6 | A | 실행 구성 선택 | 즐겨찾기 모델을 맨 위에 고정한다 | 미구현 | 1 | | 🎨✓🔒 |
| R-7 | A | 실행 구성 선택 | 모델명·프로바이더로 **모델을** 검색한다 | 미구현 | 1 | | 🎨 |
| R-8 | A | 실행 구성 선택 | 추론 강도·모드·thinking 예산을 모델과 한 화면에서 고른다 | 미구현 | 1 | | |
| R-9 | A | 실행 구성 선택 | 추론 옵션을 고르는 곳이 한 군데뿐이다 | 미구현 | 1 | | |
| F-83 | A | 기본 실행 구성 | 하네스 기본값에서 thinking 예산·Cursor 속도 등급을 설정한다 | 미구현 | 1 | | |
| R-25 | A | OpenRouter 키 관리 | 저장된 OpenRouter 키를 지운다 | 미구현 | 2 | | |
| **F-113** | A | 모델 변경 | 대화 시작 후에도 모델·하네스를 바꾸고 맥락을 이어간다 | 미구현 | 19 | **📋** | 🎨 |
| R-10 | B | 멤버 상태 표시 | 사이드바에서도 상태를 헤더와 같은 칩으로 본다 | 미구현 | 3 | **📋** | 🎨✓ |
| (UR 없음) | B | 멤버 상태 표시 | **상태별 색 팔레트를 정한다** | 미구현 | 3 | **📋** | 🎨 |
| R-18 | B | 메시지 표시 | **오구현** — 대화에 남은 이미지를 복사할 수 있어야 한다 | 오구현 | 6 | | |
| R-19 | B | 메시지 표시 | 대화 속 이미지를 원래 크기로 본다 | 미구현 | 6 | | 🎨✓ |
| **F-109** | B | 대화 검색 | 지난 대화를 검색해 그 지점으로 간다 | 미구현 | 15 | **📋** | 🎨 |
| R-63 | C | 멤버 간 통신 | **오구현** — 세션 시작 전이어도 탭이 열려 있으면 전달된다 | 오구현 | 4 | | |
| R-12 | C | 턴 중단 | Esc 로 진행 중인 턴을 멈춘다 | 미구현 | 10 | | |
| R-13 | C | 턴 중단 | 팝업이 열려 있으면 Esc 가 그것을 먼저 닫는다 | 미구현 | 10 | | |
| R-62 | C | 첨부 | 드롭한 파일이 파일명 칩으로 보인다 (호버 시 전체 경로, 모델엔 경로 그대로) — **R-81 과 같은 일. 이것만 닫으면 둘 다 닫힌다** | 미구현 | 13 | | 🎨✓🔒 |
| R-15 | C | 첨부 | 붙여넣은 이미지를 세션이 실물 파일로 다룬다 | 미구현 | 13 | **📋** | |
| R-16 | C | 첨부 | WSL·원격 멤버도 열 수 있는 경로로 받는다 | 미구현 | 13 | **📋** | |
| R-17 | C | 첨부 | 쌓인 첨부가 정리되되, 경로가 바뀌면 알려준다 | 미구현 | 13 | **📋** | |
| ~~R-81~~ | — | 첨부 | 파일 드롭 동작 — **완료. R-62 로 병합됐다. 따로 배정하지 마라** | 완료 | — | | |
| **F-107** | C | 멤버 멘션 | `@` 로 파티 멤버를 골라 문장 중간에 넣는다 | 미구현 | 14 | | 🎨✓🔒 |
| **F-114** | ⛔ | 스킬 자동완성 | `/` 스킬 — **기본 동작은 이미 완료.** 남은 개선은 무엇을 만들지 미정 | 부분 완료 | — | **📋** | |
| F-111 | D | 설정 분류 | 런타임 설정이 성격별로 묶인다 | 미구현 | 8 | **📋** | 🎨 |
| R-1 | D | 멤버 생성 | 이름과 역할을 한 단계에서 입력한다 | 미구현 | 9 | | 🎨 |
| R-2 | D | 멤버 생성 | 파티·멤버 이름에 한글을 쓴다 | 미구현 | 9 | **📋** | |
| R-3 | D | 멤버 생성 | 한글 이름이 입력한 그대로 보인다 | 미구현 | 9 | **📋** | |
| F-104 | D | 파티 보관 | 안 쓰는 **파티**를 치웠다가 되살린다 | 미구현 | 17 | **📋** | 🎨 |
| F-105 | D | 멤버 보관 | 안 쓰는 **멤버**를 치웠다가 되살린다 | 미구현 | 17 | **📋** | 🎨 |
| **F-110** | D | 파티 간 통신 | 다른 파티의 멤버에게 메시지를 보낸다 | 미구현 | 20 | **📋** | |
| **F-108** | ⛔ | 스킬 카탈로그 | 화면이 쓰기 어렵다 — **어디가 걸리는지 미특정** | 미구현 | — | **📋** | 🎨 |

**집계** — 📋 기획 필요 **14** · 🎨 디자인 필요 **9** · 🎨✓ 디자인 있음 **6** · 🔒 담당 배정 대기 **4**

> 📋 가 붙은 것은 **착수 전에 정해야 한다.** 그중 F-108 · F-114 는 정하기 전엔 분배도 안 된다.
> 🎨✓ 여섯(R-5 · R-6 · R-10 · R-19 · R-62 · F-107)은 **참고할 디자인이 이미 있다.**
>
> 🔒 **= 세 번째 종류의 막힘 — 기획도 디자인도 아니고 "누구에게 맡기느냐"다.**
> R-5 · R-6 · R-62 · F-107 넷은 **같은 디자인 핸드오프 한 벌**에서 나왔다
> (`docs/디자인 핸드오프/design_handoff_mentions_catalog`). 적용하려면 목업을 띄워 캡처하고
> 눈으로 대조해야 하는데, **지금 배정된 담당의 하네스가 이미지를 볼 수 없다.**
> **이미지를 볼 수 있는 담당 한 명에게 넷을 통째로** 주면 바로 풀린다 — 쪼개면 같은 문서를
> 네 번 읽는다.

---

# 남은 일 — 4개 레인 분배안

레인은 **건드리는 화면이 겹치지 않게** 잘랐다. 넷을 동시에 돌려도 서로 막지 않는다.

레인은 **6개**다. **N1·N2 는 지금 시작하고**, B1~B4 는 디자인·기획이 끝나면 시작한다.

| 레인 | 성격 | 건수 | 시작 |
|---|---|---|---|
| **N1** 설정·모델·전달 | 정할 것도 그릴 것도 없다 | 5 | **지금** |
| **N2** 대화·입력창 | 참고할 디자인이 이미 있다 | 6 | **지금** |
| B1 모델 카탈로그 화면 | 🎨 화면 디자인 필요 | 4 | 디자인 후 |
| B2 상태 표시·대화 검색 | 📋 어휘·범위 결정 필요 | 3 | 기획 후 |
| B3 첨부 | 📋 저장·경로·정리 정책 필요 | 4 | 기획 후 |
| B4 멤버·이름·보관·파티 간 | 📋🎨 대부분 정할 것이 있다 | 7 | 기획·디자인 후 |

---

# 지금 시작하는 2개 레인

## ▶ N1 — 설정 · 모델 · 전달

정할 것도 새로 그릴 것도 없다. **바로 착수.**

| 순 | 식별자 | 할 일 | 상태 |
|---|---|---|---|
| 1 | R-8 | 추론 강도·모드·thinking 예산을 모델과 한 화면에서 고른다 | 미구현 |
| 2 | R-9 | 추론 옵션을 고르는 곳이 한 군데뿐이다 | 미구현 |
| 3 | F-83 | 하네스 기본값에서 thinking 예산·Cursor 속도 등급을 설정한다 — **지금은 저장할 자리는 있는데 조작 수단이 없어 영원히 미설정** | 미구현 |
| 4 | R-25 | 저장된 OpenRouter 키를 지운다 | 미구현 |
| 5 | R-63 | **오구현 수정** — 세션 시작 전이어도 탭이 열려 있으면 메시지가 전달된다 | 오구현 |

> 1~3 은 **한 덩어리**다. 확인 결과 "기능 추가"가 아니라 **이미 있는 걸 켜고 중복을 지우는 일**이고,
> 이때 F-83 의 죽은 설정 2개도 함께 닫힌다.
> 5 는 화면이 아니라 전달 경로라 나머지와 안 겹친다.

## ▶ N2 — 대화 · 입력창

참고할 디자인이 이미 있거나, 디자인이 필요 없다. **바로 착수.**

| 순 | 식별자 | 할 일 | 상태 | 디자인 |
|---|---|---|---|---|
| 1 | R-18 | **오구현 수정** — 대화에 남은 이미지를 복사할 수 있어야 한다 | 오구현 | |
| 2 | R-19 | 대화 속 이미지를 원래 크기로 본다 | 미구현 | 🎨✓ |
| 3 | R-12 | Esc 로 진행 중인 턴을 멈춘다 | 미구현 | |
| 4 | R-13 | 팝업이 열려 있으면 Esc 가 그것을 먼저 닫는다 | 미구현 | |
| 5 | R-62 | 첨부가 썸네일 대신 파일명 칩으로 보인다 | 미구현 | 🎨✓ |
| 6 | **F-107** | `/` 로 스킬, `@` 로 멤버를 골라 넣는다 | 미구현 | 🎨✓ |

> 1~2 는 같은 자리(대화 속 이미지)라 함께 한다. 1 은 배선이 이미 있고 **붙는 위치만 잘못됐다.**
> 6 이 이 레인에서 가장 크다. 참고할 디자인이 있어 착수는 가능하다.

---

# 디자인·기획 후 시작하는 4개 레인

## ⏸ B1 — 모델 카탈로그 화면 (🎨 디자인 필요)

| 식별자 | 할 일 | 막는 것 |
|---|---|---|
| R-5 | 프로바이더 그룹을 접고 편다 (처음엔 접힘) | 🎨 화면 |
| R-6 | 즐겨찾기 모델을 맨 위에 고정한다 | 🎨 화면 |
| R-7 | 모델명·프로바이더로 **모델을** 검색한다 | 🎨 화면 |
| F-113 | 대화 시작 후에도 모델·하네스를 바꾸고 맥락을 이어간다 | 📋 이전 대화 이관 정책 + 🎨 |

> R-5·6·7 은 **같은 화면**이라 한 번에 그려야 한다. 따로 그리면 세 번 그린다.

## ⏸ B2 — 상태 표시 · 대화 검색 (📋 기획 필요)

| 식별자 | 할 일 | 막는 것 |
|---|---|---|
| R-10 | 사이드바에서도 상태를 헤더와 같은 칩으로 본다 | 📋 **상태 어휘 확정** (7개 제안이 승인 대기) |
| (UR 없음) | 상태별 색 팔레트를 정한다 | 📋 어휘 확정 후 + 🎨 |
| F-109 | 지난 대화를 검색해 그 지점으로 간다 | 📋 **범위** — 열린 대화 / 파티 전체 / 워크스페이스 전체 |

> 어휘 없이 칩을 만들면 **틀린 값을 더 예쁘게 보여주게 된다.** 승인 하나로 앞의 둘이 풀린다.

## ⏸ B3 — 첨부 (📋 정책 필요)

| 식별자 | 할 일 | 막는 것 |
|---|---|---|
| R-15 | 붙여넣은 이미지를 세션이 실물 파일로 다룬다 | 📋 저장 위치 |
| R-16 | WSL·원격 멤버도 열 수 있는 경로로 받는다 | 📋 경로 변환 |
| R-17 | 쌓인 첨부가 정리되되, 경로가 바뀌면 알려준다 | 📋 정리 정책 |
| R-81 | 파일 드롭 — 동작은 완료, **보이는 형태 미완** | 🎨 구체안 미정 |

> 세 정책이 한 덩어리다. 따로 정하면 서로 안 맞는다.

## ⏸ B4 — 멤버 · 이름 · 보관 · 파티 간 통신 (📋🎨)

| 식별자 | 할 일 | 막는 것 |
|---|---|---|
| R-1 | 이름과 역할을 한 단계에서 입력한다 | 🎨 화면 |
| R-2 | 파티·멤버 이름에 한글을 쓴다 | 📋 **표시명과 저장 키 분리 설계** |
| R-3 | 한글 이름이 입력한 그대로 보인다 | 📋 위와 같음 |
| F-111 | 런타임 설정이 성격별로 묶인다 | 📋 분류안 + 🎨 |
| F-104 | 안 쓰는 **파티**를 치웠다가 되살린다 | 📋 이름 재사용 정책 + 🎨 |
| F-105 | 안 쓰는 **멤버**를 치웠다가 되살린다 | 📋 위와 같음 + 🎨 |
| F-110 | 다른 파티의 멤버에게 메시지를 보낸다 | 📋 주소 체계 — **R-2·R-3 뒤** |

> 검증만 풀면 **한글 이름이 전부 `----` 로 뭉개진다.** 표시명과 저장 키를 나누는 설계가 먼저다.

## ⛔ 분배하지 않는 것

| 식별자 | 왜 |
|---|---|
| **F-108** 스킬 카탈로그 | "쓰기 어렵고 비직관적"이라는 보고만 있고 **어디가 걸리는지가 특정되지 않았다.** 찾는 단계인지·고르는 단계인지·적용하는 단계인지·화면 진입 자체인지에 따라 만들 것이 완전히 달라진다. **사용자 답변 하나면 바로 분배 가능** |

## 착수 첫날에 막히는 것 — 미리 정해야 할 4가지

| 항목 | 정해야 할 것 |
|---|---|
| F-109 대화 검색 | 범위 — 열린 대화 안 / 파티 전체 / 워크스페이스 전체 |
| F-104·F-105 보관 | 보관된 이름을 새로 만들 때 재사용할 수 있는가 |
| F-113 모델 변경 | 이전 대화 이관 정책 — 길면 요약할지 자를지, 도구 호출·이미지를 어디까지 |
| R-10 상태 칩 | 상태 어휘 확정 (7개 제안이 승인 대기) |

## 남은 결함 (요구사항 밖)

| 무엇 | 상태 |
|---|---|
| 이미 하네스로 넘어간 메시지는 취소할 수 없다 | **표시로 완화됨** — "막았다"고 잘못 믿을 여지는 없앴고, 구간 자체를 없애는 일이 남았다 |
| 좁은 폭에서 **승인 대기**가 안 보인다 | 상태 표시 체계와 함께 다룰 일 (레인 B) |
| compact 를 해도 무반응 | **재현 정보 필요** — 조사 결과 원래 보고가 틀렸다 |
| 인터럽트 전달이 멤버 쪽에 에러처럼 보인다 | 미착수 |

---

# ✅ 완료 (26)

| 식별자 | 위치 | 내용 |
|---|---|---|
| R-65 | 화면 배치 | 파티가 많아도 목록을 끝까지 스크롤해 모든 항목을 클릭할 수 있다 |
| R-66 | 멤버 관리 | 죽은 멤버는 대화할 수 없는 상태로 보인다 |
| R-64 | 멤버 관리 | 멤버가 죽으면 하네스와 무관하게 연결 끊김으로 표시된다 (Cursor 는 해당 없음) |
| R-67 | 대화 기록 | 응답이 흘러나오는 중에 메시지를 보내도 코드 블록이 온전하게 남는다 |
| R-68 | 메시지 전송 | 패널을 나눠 폭이 좁아져도 전송 키가 설정한 대로 동작한다 |
| R-69 | 화면 배치 | 모달 바깥을 눌러도 닫히지 않아 작성 중인 입력이 남는다 |
| R-70 | 멤버 런타임 | 첫 턴을 보내기 전에 하네스나 모델을 바꿔도 오류 없이 적용된다 |
| R-71 | 세션 수명주기 | 재시작한 뒤 대화를 이어가도 그때까지의 대화가 그대로 남는다 |
| R-72 | 서브에이전트 | 서브에이전트에 위임한 긴 명령이 접혀 대화창을 가리지 않는다 |
| R-73 | 권한 제어 | 화면에서 바꾼 Codex 권한이 저장되어 다음에도 그대로 남는다 |
| R-74 | 전송 동작 설정 | 메시지를 보낼 때 상대의 진행 중인 턴을 끊을지 말지를 설정으로 정한다 |
| R-75 | 전송 동작 설정 | Enter 로 보낼지 Ctrl+Enter 로 보낼지 고른다 |
| R-76 | 대화 기록 | 코드 블록과 응답 전체를 클릭 한 번으로 복사할 수 있다 |
| R-77 | 대화 기록 | 링크가 평소 쓰는 브라우저에서 열리고, 링크 주소를 복사할 수 있다 |
| R-78 | 대화 기록 | 전체 보기 창의 내용이 가장자리에 붙지 않고 여백을 갖는다 |
| R-79 | 멤버 상태 표시 | 멤버가 실제로 돌고 있는지를 글자가 아니라 움직이는 표시로 안다 |
| R-80 | 멤버 상태 표시 | 멤버가 어느 하네스로 도는지 목록과 탭에서 바로 안다 |
| R-81 | 첨부 | 파일을 입력창에 끌어다 놓으면 그 경로가 들어간다 **(표현 미완 — 레인 C)** |
| R-82 | 대기열 | 작업 중인 멤버에게 보낸 메시지가 대화에 섞이지 않고 대기열로 간다 |
| R-83 | 대기열 | 대기 중인 메시지가 몇 건인지 보인다 |
| R-84 | 대기열 | 대기열을 펼쳐 대기 중인 메시지 내용을 확인한다 |
| R-85 | 대기열 | 대기 중인 메시지를 취소하거나 편집한다 |
| R-86 | 대기열 | 대기 중인 메시지의 순서를 바꾸거나 앞 메시지와 합친다 |
| R-87 | 대기열 | 기다리지 않고 대기 중인 메시지를 지금 보낸다 |
| R-88 | 대기열 | 앱을 껐다 켜도 대기 중이던 메시지가 남아 있다 |
| R-24 | Claude 구독 | 인증 화면에서 Claude 구독 연결을 끊을 수 있다 |

## 이번 정리에서 뺀 것

| 무엇 | 왜 |
|---|---|
| 사용량 표시 · gpt 백엔드 | **이미 이전에 작업이 끝나 있던 항목.** 모르고 다시 손댔다가 뺐다. 안 중요해서가 아니다 |
