# 공유 지점 — 워크트리를 나눴다고 격리가 끝난 게 아니다

**무엇을 공유하는지 알고 있어야 한다.**

> ### 한 줄 요약 — **격리했다고 믿는 순간이 가장 위험하다.**
> 오늘의 사고는 전부 "여긴 내 것"이라고 믿은 자리에서 났다. 의심하고 있던 자리에서는
> 한 번도 안 났다.

여러 멤버가 각자 워크트리에서 병렬로 일한다. 워크트리는 **파일을 격리한다.** 그러나 파일이 아닌
것들은 여전히 하나이고, 그 하나를 건드리면 **나 혼자가 아니라 전원이 멈춘다.**

2026-08-02 하루에 "격리된 줄 알았는데 공유 지점이 있었다"가 **다섯 번** 났다. 다섯 다 형태만
달랐지 같은 계열이다. 이 문서는 그 공유 지점 목록이다.

각 항목에 **어떻게 알아채는가(증상)** 와 **누가 고치는가** 를 붙였다. 증상만 알아도 다음 사람이
30분을 아낀다.

**항목마다 무게가 다르다. 제목에 표시했다.**

| | 뜻 |
|---|---|
| **💥 발생** | **실제로 사고가 났다.** 여기 적힌 증상을 오늘 누군가 겪었다 |
| **🛡 예방** | 아직 안 났다. **구조상 날 수 있어서** 미리 막아둔 것 |
| **✅ 해결** | 근본 수정이 들어갔다. **경위는 지우지 않고 남긴다** — 지우면 그런 함정이 있었다는 것조차 안 남는다 |

💥 는 "조심하라"가 아니라 **"이미 당했다"** 는 뜻이다. 🛡 보다 먼저 읽어라.
반대로 🛡 중 **10·11 번은 한 번 나면 되돌릴 수 없다.** 설치본은 몇 초면 고쳐지지만 **죽은
세션과 지워진 실체는 안 돌아온다.** 확률이 낮다고 가벼운 것이 아니다.

**항목마다 `마지막 확인` 이 붙어 있다. 그것도 읽어라.**
날짜가 오래됐으면 **그 사이 뒤집혔을 수 있다**(→ 읽는 법). `미확인` 은 **"안전하다"가 아니라
"아무도 확인해 보지 않았다"** 는 뜻이다 — 시험하는 순간이 사고라서 확인할 수 없는 것도 있다.
일부 항목에는 **⚠️ 로 "겪은 것"과 "구조에서 추론한 것"을 갈라 두었다.** 추론은 사실보다 약하다.

---

## 이 문서를 읽는 법 — 여기 적힌 것도 언젠가 뽑은 사진이다

아래 목록은 **공간의 문제**다: 격리된 줄 알았는데 공유 지점이 있었던 것.
축이 하나 더 있다 — **시간의 문제**다: **그때는 맞았는데 지금은 아닌 것.**

목록·문서·조사 결과는 **뽑은 순간의 사진**이지 살아있는 상태가 아니다. 특히
**"무엇이 있고 없는지"는 시간이 지나면 뒤집힌다.** 판단의 근거로 삼기 전에 **지금도 그런지
확인해라.**

> 실제 사례: 어떤 항목에 "디자인이 없다"고 단정한 판단이 있었다. 근거는 몇 시간 전에 뽑은
> 핸드오프 폴더 목록이었고, **그 사이 확정본이 추가돼 있었다.** 목록은 틀리지 않았다 —
> **낡았을 뿐이다.** 그리고 낡은 것은 틀린 것과 똑같이 사람을 잘못 이끈다.

**이 문서를 믿고 확인을 건너뛰면, 이 문서가 다음 사고의 원인이 된다.**
쓸 때 추정하지 않는 것만으로는 부족하다. **읽을 때도 확인해야 한다.**

---

## 이 문서를 고치는 법 — 한 곳만 고치면 나머지가 조용히 반대말을 한다

**한 곳을 고쳤으면 그 이름이 나오는 모든 곳을 검색해라.** 문서는 여러 절이 같은 사실을 각자
들고 있어서, **한 절만 고치면 나머지가 조용히 반대말을 한다.** 그리고 반대말을 하는 쪽이
틀렸다는 표시는 어디에도 없다.

> 실제 사례: 병합돼 사라진 요구사항을 표에서 지웠는데 **다른 절에 같은 항목이 살아 있었다.**
> 지운 사람이 "지웠다"고 보고까지 한 뒤였다. 그대로 뒀으면 **두 레인이 같은 것을 각자
> 만들었을 것이다.** 유령을 남기지 말라고 지시한 사람과 지운 사람이 같은 편이었는데도 남았다.

**어디에 적을 것인가** — 경계는 하나다:
- **사용자가 겪는 것이 바뀌면 → Canopy** (요구사항·세부기능)
- **우리가 일하는 방식이 바뀌면 → 이 문서** (공유 지점·운영 규칙)

e2e 하드코딩, 설치본 공유, 단위 테스트 충돌은 **전부 후자**다. 고쳐도 사용자가 겪는 것은
하나도 안 바뀐다. 반대로 "인터럽트가 에러처럼 보인다"는 전자다 — 고치면 사용자가 보는 것이
달라진다.

**항목이 해결되면 지우지 말고 갱신해라.** 무엇이 아직 위험하고 무엇이 해결됐는지가 여기 있어야
한다. 지워버리면 **다음 사람은 그 함정이 있었다는 것조차 모른다.**

---

## 💥 1. 설치본(툴체인) — 전 워크트리가 폴더 하나를 공유한다

**마지막 확인 2026-08-02** — 워크트리 5곳의 `node_modules` 가 전부 주 폴더를 가리키는 심링크임을 직접 확인했다.

각 워크트리의 `node_modules` 는 실제 폴더가 아니라 **주 폴더를 가리키는 심링크**다.
2026-08-02 확인:

```
AgentPartyApp        node_modules  (실체 — 여기 하나뿐이다)
AgentPartyApp-n1     node_modules -> C:\Project\AgentPartyApp\node_modules
AgentPartyApp-n2     node_modules -> (같음)
AgentPartyApp-b1     node_modules -> (같음)
AgentPartyApp-queue  node_modules -> (같음)
AgentPartyApp-w1     node_modules -> (같음)
```

**파일만 격리되고 툴체인은 공유다.** 내 워크트리에서 의존성을 건드려도 그 영향은 내 워크트리에
머무르지 않는다.

**증상** — 빌드·타입체크·테스트가 갑자기 `패키지를 찾을 수 없음` / `tsc 를 찾을 수 없음` 으로
죽는다. 내가 아무것도 안 바꿨는데 죽었다면 십중팔구 이것이다. **한 명이 아니라 전원이 동시에**
겪는 것이 결정적 단서다.

**누가 고치는가 — main 만.** 의존성 폴더를 건드리는 명령은 **이름이 무엇이든** 금지다:
설치·재설치·정리·링크 재생성 전부. **"고치려고" 돌리는 것도 포함이다.**

> ⚠️ 이게 왜 예외 없는 규칙인가: 오늘 두 번째 고장은 **복구를 맡은 멤버가 정석대로 복구를
> 시도하다** 났다. 그 도구가 공유 상태를 바꾼다는 걸 실행 전에 아무도 예상하지 못했다.
> **고치는 행동이 고장의 원인이 될 수 있다.** 그래서 한 사람만 손대야 한다.

깨진 것 같으면 **아무것도 실행하지 말고 main 에게 알려라.** 몇 초면 고쳐진다.

---

## 💥 2. 실행 중인 앱 — 재설치를 막지만, 끌 수도 없다

**마지막 확인 2026-08-02** — 복구를 맡은 멤버가 재설치를 시도하다 실제로 막혔다(보고 기준). "앱이 파일을 붙잡는다"는 실패 메시지까지는 내가 직접 보지 않았다.

**탈출구가 없는 조합이다.** 이걸 모르면 "정석대로 전체 재설치"를 시도하다 상태를 더 깨뜨린다.

- 앱이 떠 있으면 **실행 파일을 붙잡고 있어 재설치가 실패한다**
- 그렇다고 앱을 끌 수 없다 — **앱을 끄면 우리 세션이 전부 죽는다**

**증상** — 설치·복구 명령이 파일 접근/사용 중 오류로 중간에 끊긴다. 그리고 그 **중간에 끊긴
설치가 바로 1번 고장의 정체**다. 즉 이 조합은 사고의 결과가 아니라 **원인**이다.

**누가 고치는가 — 근본 복구는 앱이 꺼져 있을 때만 가능하다.** main 이 사용자에게 창을 요청해
처리한다. 그전까지는 반쪽 상태 그대로 진행한다.

---

## 💥 3. e2e 가 남의 코드를 테스트하고 통과한다

**마지막 확인 2026-08-02** — `runtime.appRoot` 단언이 실제 스크립트 3개와 `docs/E2E_TESTING.md` §1b 에 있는 것을 확인했다. `path.sep` 가드도 코드에 있다.

스크립트가 앱 루트를 하드코딩하면 **다른 워크트리의 빌드를 구동하면서 초록을 낸다.** 내 변경이
반영되지 않았는데 통과하므로, **가장 위험한 형태의 거짓 초록**이다.

**어떻게 막는가** — 드라이버가 스스로 정한 값이 아니라 **앱이 자기 코드를 어디서 로드했는지
스스로 말한 값**을 봐야 한다. `GET /api/state` 의 `runtime.appRoot` 가 그것이다.
step 0 에서 자기 워크트리 아래인지 단언한다.

```js
const appRoot = (await get("/api/state")).runtime?.appRoot || "";
const within = (appRoot + path.sep).toLowerCase()
  .startsWith(root.toLowerCase() + path.sep);
```

> ⚠️ **`path.sep` 를 빼면 안 된다.** 경로가 문자열 접두사 관계라
> `C:\Project\AgentPartyApp` 는 `C:\Project\AgentPartyApp-n1` 의 접두사다.
> 단순 `startsWith` 로 비교하면 **메인 빌드를 자기 것으로 오인**한다.

**증상** — 코드를 고쳤는데 결과가 안 변한다. 혹은 실패해야 할 것이 통과한다.
`FEEDBACK.md` 에 실제 사례가 있다 — `appRoot` 가 baseline 실행이 다른 워크트리 빌드였음을
step 0 에서 증명했다.

**누가 고치는가 — 스크립트를 만든 레인.** 기존 예시는
`scripts/e2e-composer-input.mjs` · `e2e-transcript-render.mjs` · `e2e-message-queue.mjs`.
자세한 규칙은 `docs/E2E_TESTING.md` §1b, `docs/API.md` `runtime.appRoot` 절.

---

## ✅ 4. 단위 테스트가 공유 폴더에 고정 이름으로 중간 산출물을 썼다 — **고쳐짐**

**마지막 확인 2026-08-02** — 근본 수정이 master 에 병합됐다(`0a249bb`). 각 워크트리가 **자기 안에** 중간 산출물을 쓴다. 더는 서로의 코드를 검증하지 않는다.

단위 테스트는 각자의 소스를 즉석에서 묶어 **공유 의존성 폴더 안**에 임시 모듈을 만든 뒤 그것을
불러온다. 자리는 `node_modules/.qa/` 이고, 2026-08-02 확인 시 **103개**가 쌓여 있었다.

**파일명이 레인이 아니라 테스트 주제 기준이다** — `claude-adapter.mjs`, `codex-composer.mjs`
처럼. 그래서 **두 레인이 같은 테스트를 동시에 돌리면 A 가 방금 쓴 파일을 B 가 불러온다.**
B 는 자기 코드가 아니라 **A 의 코드를 검증하고 초록을 본다.** 쓰이는 도중이면 반쪽을 읽는다.

3번과 같은 계열이다. 원인이 하드코딩 경로에서 공유 폴더로 바뀌었을 뿐,
**초록이 근거가 안 된다는 점이 같다.**

**증상** — **내가 고치지 않은 것이 초록이나 빨강으로 나온다.** 고친 것이 반영되지 않는다.
같은 테스트를 다시 돌리면 결과가 바뀐다.

**대응 — 고쳐졌다. 다만 완화는 조건부다. 정확히 읽어라.**

- **수정을 받은 워크트리끼리는 동시에 돌려도 된다.** 순번이 필요 없다
- **아직 안 받은 워크트리는 자기들끼리 여전히 충돌한다.** 다만 **받은 쪽과는 충돌하지 않는다** —
  쓰는 자리가 갈라졌기 때문이다

**→ 그러니 할 일은 하나다: `master` 를 rebase 해라.** 받는 순간부터 순번 없이 돌려도 된다.
아직 안 받은 레인만 main 에게 순번을 요청한다. 전원이 받으면 순서제는 완전히 풀린다.

**회귀 방지** — **상시 검사**가 들어갔다. 그 자리가 다시 공유로 돌아가면 **즉시 빨강**이다.
사람이 기억해서 지키는 규칙이 아니라 **도구가 잡는다.**

> ✅ **왜 이 수정을 믿는가 — 논리가 아니라 실행으로 증명됐다.**
> 두 곳에서 같은 테스트를 **동시에** 돌려 각자 자기 소스만 보는 것을 확인했고, 그다음
> **일부러 한 자리를 공유시켜 고장을 재현**했다(한쪽이 6번 모두 상대 코드를 읽었다).
>
> **재현 못 하는 검사는 "고장이 없는 것"과 "고장을 못 보는 것"을 구분하지 못한다.**
> 오늘 우리가 네 번 당한 게 정확히 그 차이였다. **고쳤다고 말하려면 먼저 고장을 만들어
> 보일 수 있어야 한다.**

**남아 있는 것** — 공유 폴더의 **옛 임시 파일 100여 개**는 이제 아무도 안 쓴다. 지우려 했으나
안전장치에 막혀 그대로 뒀다. **무해하다. 아무도 손대지 마라 — 1번 규칙 대상이다.**

**영향 없던 것** — **실앱 e2e**(자기 워크트리 빌드를 띄우므로 이 공유 지점을 지나지 않았다.
**기능 검증은 이쪽이 근거다**), 타입체크, 빌드.

---

### 고쳐지기 전에 무엇을 했는지 (지우지 않고 남긴다)

**순서제** 였다 — 단위 테스트는 한 번에 한 레인만, 돌리기 전에 main 에게 순번을 받았다.
그리고 **순번을 받고 돌린 실행만 유효**하며 그 이전의 통과는 시간대와 무관하게 전부 무효로 쳤다.
"그때는 아무도 안 돌렸던 것 같다"는 **기억이지 근거가 아니었기** 때문이다.

이 방편에는 대가가 있었다 — **순번을 쥔 채 멈추면 뒤가 전부 섰다.** 실제로 한 멤버가 게이트에
막혀 응답을 못 하는 바람에 순번이 묶였다(→ 8번).

**이 대목을 지우지 않는 이유**: 지우면 다음 사람은 **그런 함정이 있었다는 것조차 모른다.**
비슷한 공유 자원이 또 나오면 **순서제가 임시 방편으로 다시 쓸 수 있는 카드**라는 것도 남는다.

---

## 💥 5. 세션의 현재 폴더가 주 폴더다

**마지막 확인 2026-08-02** — 오늘 두 멤버가 겪었다(보고 기준).

세션은 `C:\Project\AgentPartyApp` 에서 시작한다. **자기 워크트리가 아니다.**
의식하지 않으면 남의 자리에 쓴다.

**증상** — 주 폴더에 정체불명의 임시 파일이 생긴다. 엉뚱한 브랜치의 코드를 읽고 있다.
내 커밋에 남의 파일이 딸려 들어온다. 오늘 두 멤버가 겪었다(한 명은 임시 파일 7개, 한 명은
다른 브랜치를 봄).

**누가 고치는가 — 각자.** 명령 전에 `pwd` 로 확인한다. 워크트리는
`C:\Project\AgentPartyApp-<레인>` 이다. 이미 만든 파일이 있으면 자기 워크트리로 옮기거나 지운다.

> 예외: `canopy` 는 워크트리가 없다. 산출물이 `docs/` 문서와 Canopy 데이터뿐이라 주 폴더에
> 쓰는 것이 정상이다. 그 외 멤버가 주 폴더에 쓰고 있다면 잘못된 것이다.

---

## 🛡 6. Canopy 티켓 번호는 자동 채번이다

**마지막 확인 2026-08-02** — 타입별 자동 채번이라는 규칙은 Canopy 데이터 가이드에서 확인했다. ⚠️ **동시 쓰기로 번호가 실제로 어긋나는 것은 아직 겪지 않았다** — 규칙에서 추론한 위험이다.

Canopy 의 `ticket_number` 는 **프로젝트별·타입별로 자동 부여**된다. 여러 멤버가 동시에 REST 로
쓰면 **번호가 어긋난다.** 파일에서 벌어진 일이 데이터에서 벌어지는 것이다.

**증상** — 번호가 비거나 겹친다. 남이 방금 만든 노드와 내 노드가 같은 번호를 갖는다.

**누가 고치는가 — `canopy` 단일 창구.** 고칠 것이 있으면 넘겨라. 넘길 때 세 가지를 붙이면
되묻지 않는다:

- **티켓 번호로 지목**(`R-62`, `F-107`) — 제목은 바뀐다. 실제로 F-107 은
  「입력 자동완성」에서 「멤버 멘션」으로 바뀌었다
- **코드로 확인한 것인지, 추정인지**
- 완료라면 **병합까지 갔는지** — 담당자가 끝낸 것과 master 에 들어간 것은 다르다.
  그 구간을 위해 `병합 대기` 상태가 따로 있다

---

## 💥 7. 공용 배선은 한 레인이 늘리는 것이 아니다

**마지막 확인 2026-08-02** — 오늘 한 레인이 이름을 지어내 e2e 를 짰다가 갈아엎었다(보고 기준). `src/shared/apiSpec.ts` 가 단일 소스라는 것은 `AGENTS.md` 에 명시돼 있다.

자동화 HTTP API 의 단일 소스는 **`src/shared/apiSpec.ts`** 다. 없는 엔드포인트를 있을 법한
이름으로 **추정해서 쓰면 안 된다.**

**증상** — e2e 가 404 로 죽거나, 더 나쁘게는 존재하지 않는 기능을 검증했다고 착각한다.
오늘 한 레인이 이름을 지어내 e2e 를 짰다가 전부 갈아엎었다.

**누가 고치는가 — 없으면 없는 것이다.** 정말 필요하면 만들기 전에 main 에게 묻는다.

---

## 💥 8. 통신 경로 — 말이 안 나가는데 멈춘 것처럼 보인다

**마지막 확인 2026-08-02** — 게이트 반려로 한 멤버가 유휴로 오해받은 사례, 그리고 Cursor 권한 건 **둘 다 오늘 확인됐다.**

멤버가 보내는 메시지는 **Message Gate** 를 지난다. 게이트가 반려하면 **메시지는 전달되지 않고**
보낸 쪽만 그 사실을 안다. 받는 쪽에서는 **아무 말이 없는 것과 구별되지 않는다.**

**증상** — **멤버가 멈춘 것처럼 보인다.** 실제로는 **일이 끝났고 보고하려던 중인데 말이 안 나가고
있는 것**이다. 2026-08-02 에 실제로 이것 때문에 한 멤버가 유휴로 오해받아 순번을 넘겼다.
그 멤버는 이미 작업을 끝낸 상태였다.

**대응 (보내는 쪽)** — **두 번 막히면 같은 내용을 다시 다듬어 보내지 마라.**
`"게이트에 막혀 못 보낸다"` **한 줄만 보내라. 그건 통과한다.** 내용을 못 전해도
**살아 있다는 사실은 전해진다.** 그 뒤는 받는 쪽이 판단한다.

**대응 (받는 쪽)** — 조용한 멤버를 유휴로 단정하기 전에 `member-status` 로 확인한다.
조용함은 **"할 말이 없다"가 아니라 "말이 막혔다"** 일 수 있다.

> **같은 증상, 다른 원인이 하나 더 기록돼 있다.** Cursor 멤버는 **권한이 `auto-review` 이면
> 파티 send 자체가 막힌다.** 증상이 게이트 반려와 똑같이 "멤버가 멈춘 듯 조용함"이라
> 구별되지 않는다. **조용한 Cursor 멤버는 권한 설정도 함께 확인하라**(자율 실행은
> `unrestricted`). **오늘 확인된 사실이다** — 보고가 안 들어와 유휴로 오해했고, 권한을
> `unrestricted` 로 바꾸자 **그 직후 보고가 정상으로 들어왔다.** 증상 → 원인 → 수정 →
> 회복까지 하루 안에서 확인됐다.

**누가 고치는가** — 게이트 규칙 자체는 main. 개별 메시지는 보내는 사람이 위 방법으로 뚫는다.
`force` 는 쓰지 마라 — 막혔다는 사실 자체를 알리는 편이 낫다.

---

## 🛡 9. 워크스페이스와 파티 데이터 — 두 프로세스가 같은 파티를 열면 나중 쪽이 이긴다

**마지막 확인 2026-08-02** — `AGENTPARTY_WORKSPACE` 가 `src/` 에 **0건**임을 직접 확인했다(죽은 노브가 맞다). ⚠️ **두 프로세스가 같은 파티를 열었을 때 나중 쪽이 이기는 것은 아직 겪지 않았다** — 저장 구조에서 추론한 위험이다.

워크트리는 파일을 격리하지만 **파티 데이터는 워크스페이스에 있다.** 두 프로세스가 같은
워크스페이스를 열면 **나중에 쓴 쪽이 이긴다.** 확인용 앱을 띄울 때 **우리 파티가 사는
워크스페이스를 열면 안 되는** 이유가 이것이다.

**증상** — 멤버 설정이나 대화가 **이유 없이 되돌아간다.** 내가 바꾼 적 없는 값이 예전 값으로
돌아가 있다.

**대응** — 확인용 앱은 **격리 userData + 별도 워크스페이스**로 띄운다.

> ⚠️ 워크스페이스는 그 userData 의 **`settings.json` 의 `workspacePath` 로만** 정해진다.
> **환경변수는 앱이 읽지 않는다 — 죽은 노브다.** 환경변수로 지정했다고 믿으면 앱은
> 조용히 **원래 워크스페이스**를 연다. 즉 격리했다고 믿는 순간 가장 위험하다.

**누가 고치는가** — 앱을 띄우는 사람. main 이 조정한다.

---

## 🛡 10. 실행 중인 프로세스 — 일괄 종료 금지

**마지막 확인 — 미확인.** 겪은 적이 없다. 일괄 종료가 우리 앱까지 죽인다는 것은 구조에서 자명하지만 **실제로 시험해 볼 수 없는 종류**다(시험하는 순간이 사고다).

`taskkill /IM electron.exe /F` 같은 **전체 종료는 절대 금지**다. **우리가 사는 앱까지 죽는다.**
자기가 띄운 **프로세스 트리만** 다뤄야 한다.

**증상** — 종료한 적 없는 세션이 갑자기 사라진다.

1번·2번과 같은 계열이지만 **되돌릴 수 없다는 점이 다르다.** 설치본은 몇 초면 고쳐지지만
**죽은 세션은 돌아오지 않는다.** 진행 중이던 작업과 맥락이 함께 사라진다.

**누가 고치는가** — 고칠 수 없다. **막는 수밖에 없다.**

---

## 🛡 11. 워크트리를 지우는 것 — 심링크를 따라가면 실체가 지워진다

**마지막 확인 2026-08-02** — 심링크라는 전제는 1번에서 직접 확인했고, 오늘 워크트리 정리가 이 위험 때문에 차단됐다. ⚠️ **삭제 도구가 실제로 링크를 따라가 실체를 지우는지는 확인하지 않았다** — 확인하려면 사고를 내야 한다.

`node_modules` 가 **주 폴더를 가리키는 심링크**이므로(1번), 워크트리를 지우는 도구가 **그 링크를
따라가면 주 폴더의 실체를 지운다.** 워크트리 **하나**를 치우려다 **전원의 툴체인이 날아간다.**

**대응** — **링크를 먼저 떼어낸 뒤** 워크트리를 지운다. **순서가 뒤집히면 복구가 몇 초로
끝나지 않는다.**

**누가 고치는가 — main 만.**

> 실제로 2026-08-02 에 병합이 끝난 워크트리를 정리하려다 **이 위험 때문에 차단됐다.
> 차단이 옳았다.** 🛡 이지만 **한 번 나면 되돌리기 가장 비싼 항목**이다.

---

## 🛡 12. `master` 브랜치

**마지막 확인 — 미확인.** 운영 규칙이며 위반 사례는 아직 없다.

브랜치도 공유 지점이다. **레인은 자기 브랜치에만 커밋한다. `master` 에 쓰는 것은 main 뿐이고,
병합 순서도 main 이 정한다.**

**증상** — **내가 하지 않은 변경이 내 브랜치에 있다.** 방금 고친 것이 남의 커밋에 덮여 있다.

**누가 고치는가 — main.** 병합이 필요하면 요청하고, 직접 `master` 를 건드리지 않는다.

---

## 하루에 다섯 번에서 배운 것

**"초록을 봤다"가 아니라 "무엇을 근거로 초록이라 하는가"를 물어야 한다.**

2026-08-02 에 **초록이 거짓이었던 것이 네 번**이었다. 그리고 **네 번 다 누군가 자기 결과를
스스로 의심해서 잡혔다.** 도구가 잡아준 것이 한 번도 없다. 통과했다는 사실보다 **무엇이 그 통과를 만들었는지**가
근거다 — 어느 빌드였는지, 어느 코드가 묶였는지, 도구가 실제로 있었는지.

거짓 초록의 세 가지 형태가 위에 다 있다:
- **1번** — 도구가 없어서 아예 실행되지 않았는데 초록으로 보였다
- **3번** — 다른 워크트리의 빌드를 검증하고 초록이 나왔다
- **4번** — 다른 레인의 코드를 검증하고 초록이 나왔다 **(✅ 고쳐짐)**

**고쳤다고 말하려면 고장을 재현해 보여라.** 4번을 고친 담당이 그렇게 했다 — 먼저 정상을
확인하고, **일부러 고장을 만들어 재현한 뒤**, 회귀를 잡는 상시 검사를 넣었다.
**재현 못 하는 검사는 "고장이 없는 것"과 "고장을 못 보는 것"을 구분하지 못한다.**
오늘 네 번의 거짓 초록이 정확히 그 차이였다.

**그리고 "안 한 것"에는 항상 이유를 붙여라.** **빠뜨린 것과 의도적으로 제외한 것이 겉으로는
구별되지 않는다.** 이번 사이클에 반복해서 나왔다 — 모델 검색의 티어 축(데이터가 없어서 뺌),
파일 드롭(동작만 되고 표현은 미완), Cursor 사망 판정(고칠 것이 없어서 안 고침). 셋 다 이유를
안 적었으면 다음 사람이 **없는 일을 다시 시작**했을 것이다.

---

## 새 공유 지점을 발견하면

여기에 추가하라. 판단 기준은 하나다 — **내 워크트리 밖에 영향을 주는가.**
주면 공유 지점이고, 그러면 "누가 고치는가"를 반드시 한 사람으로 정해야 한다.

발견했더라도 **직접 고치지 마라.** 오늘 가장 값진 교훈이 그것이다 —
**복구 도구 자체가 공유 상태를 바꿔 두 번째 고장을 냈다.** 알리고, 한 사람이 고친다.
