node_modules/
dist/
dist-renderer/
release/
# Generated design-system bundle + its verification shots (docs/DESIGN_MIRROR.md).
# Always reproducible from the app itself, and 1.9 MB of it is copied fonts.
build/
!build/
build/*
!build/installer.nsh
!build/icon-source.png
!build/icon.png
!build/icon.ico
!build/icons/
!build/icons/*.png
*.log
.env
.env.*
.DS_Store
.agentparty/
.agent_party_app/
.codex/
새 폴더/

# Build artifacts / temp / binary research
release-pkg/
scripts/_tmp_*
*.xlsx
release-out/

# Local scratch / e2e run artifacts
.tmp/
# Bundles the QA scripts build so they can import their own TypeScript. Kept
# per-worktree on purpose — see scripts/lib/qaTemp.mjs.
.qa/
# Throwaway checkout + probe the collision proof provisions and removes itself.
.qa-collision-checkout/
.qa-collision-shared/
collision-probe.run.mjs
# Runtime logs an app run leaves in the repo root
logs/

# Throwaway checkouts the QA scripts provision for themselves.
.qa-showcase/
.qa-collision-checkout/
